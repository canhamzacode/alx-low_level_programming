#include <stdio.h>
/**
 * main - prints it name then a newline
 * @argc: number of command line arguments
 * @argv: array containing the aruments
 * Return: 0 -sucess
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
